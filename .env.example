# =============================================================================
# MedTech QMS Automation - Environment Configuration
# =============================================================================
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요.
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Google AI (Gemini) - LLM 및 임베딩용
# -----------------------------------------------------------------------------
# Google AI Studio에서 발급: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-gemini-api-key-here

# -----------------------------------------------------------------------------
# Google OAuth 2.0 - Drive/Sheets 연동용
# -----------------------------------------------------------------------------
# Google Cloud Console에서 생성: https://console.cloud.google.com/apis/credentials
# 애플리케이션 유형: Desktop App
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# OAuth 리디렉션 URI (기본값 사용 권장)
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# -----------------------------------------------------------------------------
# Google Drive 폴더 설정
# -----------------------------------------------------------------------------
# QMS 루트 폴더 이름 (Google Drive 내)
DRIVE_QMS_ROOT_FOLDER=QMS_ROOT

# SOP 문서 폴더 이름 (QMS 루트 하위)
DRIVE_SOP_FOLDER=01_SOPs

# -----------------------------------------------------------------------------
# Gemini 모델 설정
# -----------------------------------------------------------------------------
# LLM 모델 (채팅/분석용)
# 앱 실행 후 UI에서 사용 가능한 모델 목록 확인 가능
# 예: gemini-2.0-flash, gemini-1.5-pro, gemini-1.5-flash
GEMINI_MODEL=gemini-2.0-flash

# 임베딩 모델 (문서 벡터화용)
# 예: text-embedding-004, embedding-001
GEMINI_EMBEDDING_MODEL=text-embedding-004

# Temperature (0.0 ~ 2.0, 낮을수록 일관된 응답)
GEMINI_TEMPERATURE=0.2

# -----------------------------------------------------------------------------
# Google Sheets 설정 (선택사항)
# -----------------------------------------------------------------------------
# QMS 관리 시트 ID (URL에서 /d/ 뒤의 값)
# 예: https://docs.google.com/spreadsheets/d/ABC123.../edit → ABC123...
QMS_SHEET_ID=

# 시트 탭 이름
QMS_SHEET_RELEASE_TAB=릴리스현황
QMS_SHEET_VV_TAB=V&V검증

# -----------------------------------------------------------------------------
# 애플리케이션 설정
# -----------------------------------------------------------------------------
# 로그 레벨: debug | info | warn | error
LOG_LEVEL=info

# LLM 타임아웃 (밀리초)
LLM_TIMEOUT_MS=30000

# 실행 환경: development | production | test
NODE_ENV=development

# 환경변수 검증 엄격 모드 (production에서는 true 권장)
STRICT_ENV_VALIDATION=false
